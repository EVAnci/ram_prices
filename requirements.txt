playwright==1.48.0
playwright-stealth==1.0.6
# IMPORTANTE: numpy >= 2.4 compila con baseline SSE4.2 obligatorio (sin
# fallback), que el Atom N2600 (máx. SSSE3) no soporta -> "Instrucción
# ilegal" al importar. Fijado <2.4 para forzar el build con baseline SSE3.
numpy<2.4
pandas==2.2.3
matplotlib==3.9.2
requests
